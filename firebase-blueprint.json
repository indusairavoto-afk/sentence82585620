{
  "entities": {
    "GlobalStats": {
      "title": "Global Stats",
      "description": "Tracks global statistics for the platform",
      "type": "object",
      "properties": {
        "visitors": {
          "type": "number",
          "description": "Total number of unique visitors"
        },
        "uses": {
          "type": "number",
          "description": "Total number of bridged conversions or uses"
        },
        "donationCount": {
          "type": "number",
          "description": "Total number of people who clicked donate"
        }
      },
      "required": ["visitors", "uses", "donationCount"]
    },
    "VaultItem": {
      "title": "Vault Item",
      "description": "Saved workflow item containing chat data",
      "type": "object",
      "properties": {
        "id": {
          "type": "string"
        },
        "date": {
          "type": "number"
        },
        "data": {
          "type": "object"
        }
      },
      "required": ["id", "date", "data"]
    }
  },
  "firestore": {
    "/stats/{statId}": {
      "schema": {
        "$ref": "#/entities/GlobalStats"
      },
      "description": "Global statistics collection. Usually only a single document 'global'."
    },
    "/users/{userId}/vault/{vaultId}": {
      "schema": {
        "$ref": "#/entities/VaultItem"
      },
      "description": "User's individual saved chats."
    }
  }
}
